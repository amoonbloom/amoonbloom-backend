node_modules
# Keep environment variables out of version control (never commit real secrets).
# .env.example IS tracked as a template (it doesn't match the ".env" line below).
.env
.env.local
.env.*.local

# Prisma
/generated/prisma

nul
temp_auto_push.bat
temp_interactive_push.bat

.report-previews/
config.bat

# Database dumps / backups — never commit (may contain production data)
backups/
*.dump
